/*
import { mount } from '@vue/test-utils'
import FlashCard from '@/components/FlashCard'

describe('Testing DeckList.vue', () => {
  it('should render a deck card for each deck', () => {
    // when

    const wrapper = mount(FlashCard, {
      propsData: {
        cards: [
          {
            deckId: 1,
            id: 1,
            frontInformation: 'Christopher',
            backInformation: 'Chris'
          }
        ]
      }
    })

    const cardFront = wrapper.find('.info')
    expect(cardFront.text()).toBe('Christopher')
    // then
  })
})
*/
